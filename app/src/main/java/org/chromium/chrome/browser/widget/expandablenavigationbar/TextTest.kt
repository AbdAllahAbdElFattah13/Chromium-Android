package org.chromium.chrome.browser.widget.expandablenavigationbar

import android.content.Context
import android.widget.TextView

class TextTest(context: Context?) : TextView(context) {
    init {
        this.text = "Boda!"
    }
}